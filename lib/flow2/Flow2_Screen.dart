import 'package:flutter/material.dart';
class Flow2Screen extends StatelessWidget {
  const Flow2Screen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}